name: Build & Push FX AI EA v5 (700特徴量対応イメージ)

on:
  push:
    paths:
      - 'DOK/Dockerfile.ea5'
      - 'DOK/entrypoint_ea5.sh'
      - 'DOK/requirements.txt'
      - 'DOK/ssh/**'
      - 'fx-ea5/**'
      - '.github/workflows/build-dok-ea5.yml'
  workflow_dispatch:

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    timeout-minutes: 120

    steps:
      - name: Free disk space
        run: |
          sudo rm -rf /usr/share/dotnet /usr/local/lib/android /opt/ghc \
                      /opt/hostedtoolcache/CodeQL /usr/local/share/boost \
                      "$AGENT_TOOLSDIRECTORY"
          sudo docker image prune --all --force
          df -h

      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and Push frtgroup/fx-ea5:latest
        uses: docker/build-push-action@v6
        with:
          context: .
          file: DOK/Dockerfile.ea5
          push: true
          provenance: false
          sbom: false
          tags: |
            ${{ secrets.DOCKERHUB_USERNAME }}/fx-ea5:latest
            ${{ secrets.DOCKERHUB_USERNAME }}/fx-ea5:${{ github.sha }}
          platforms: linux/amd64
          build-args: |
            GDRIVE_OAUTH_CLIENT_ID=${{ secrets.GDRIVE_OAUTH_CLIENT_ID }}
            GDRIVE_OAUTH_CLIENT_SECRET=${{ secrets.GDRIVE_OAUTH_CLIENT_SECRET }}
            GDRIVE_OAUTH_REFRESH_TOKEN=${{ secrets.GDRIVE_OAUTH_REFRESH_TOKEN }}

      - name: Summary
        run: |
          echo "## ✅ FX AI EA v5 (700特徴量) イメージ ビルド完了" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| 項目 | 値 |" >> $GITHUB_STEP_SUMMARY
          echo "|------|---|" >> $GITHUB_STEP_SUMMARY
          echo "| イメージ | \`${{ secrets.DOCKERHUB_USERNAME }}/fx-ea5:latest\` |" >> $GITHUB_STEP_SUMMARY
          echo "| 特徴量   | 700グループ × 25列 = 17500次元 |" >> $GITHUB_STEP_SUMMARY
          echo "| 対応環境 | GTX / RTX / H100 / H200 / TPU (起動時自動検出) |" >> $GITHUB_STEP_SUMMARY
          echo "| ダッシュボード | ポート \`8080\` |" >> $GITHUB_STEP_SUMMARY
          echo "| SSH | ポート \`22\` |" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 起動コマンド" >> $GITHUB_STEP_SUMMARY
          echo "**GPU (Vast.ai / ローカル):**" >> $GITHUB_STEP_SUMMARY
          echo "\`\`\`bash" >> $GITHUB_STEP_SUMMARY
          echo "docker run --gpus all --privileged --net=host ${{ secrets.DOCKERHUB_USERNAME }}/fx-ea5:latest" >> $GITHUB_STEP_SUMMARY
          echo "\`\`\`" >> $GITHUB_STEP_SUMMARY
          echo "**TPU (Google Cloud TPU VM):**" >> $GITHUB_STEP_SUMMARY
          echo "\`\`\`bash" >> $GITHUB_STEP_SUMMARY
          echo "docker run --privileged --net=host ${{ secrets.DOCKERHUB_USERNAME }}/fx-ea5:latest" >> $GITHUB_STEP_SUMMARY
          echo "\`\`\`" >> $GITHUB_STEP_SUMMARY
