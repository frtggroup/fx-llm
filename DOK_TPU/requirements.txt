# ── Web dashboard / API ───────────────────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# ── ONNX (CPU 推論エクスポート用) ────────────────────────────────────────────
onnx>=1.16.0
onnxruntime>=1.18.0

# ── Data / ML ────────────────────────────────────────────────────────────────
numpy>=1.26.0
pandas>=2.2.0
scipy>=1.12.0
scikit-learn>=1.4.0

# ── Misc ─────────────────────────────────────────────────────────────────────
tqdm

# ── Object Storage ────────────────────────────────────────────────────────────
boto3>=1.34.0

# ── Google Drive 共有ストレージ ───────────────────────────────────────────────
google-api-python-client>=2.130.0
google-auth>=2.29.0
google-auth-httplib2>=0.2.0

# ── TPU / XLA (torch_xla はベースイメージに含まれるため追加不要) ─────────────
# torch_xla は us-central1-docker.pkg.dev/tpu-pytorch/torch-xla-release イメージ内に含まれる
